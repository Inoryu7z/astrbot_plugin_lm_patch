"""LLM 提示词模板"""

import json
import re

# ============================================================
# 功能 1：人设补丁
# ============================================================

PERSONA_PATCH_SYSTEM_PROMPT = """你是一个人设维护助手。你会收到一个聊天机器人角色的当前人设（系统提示词），以及该角色最近积累的新记忆。

你的任务是：判断这些新记忆是否揭示了需要更新人设的信息。如果需要，输出完整更新后的人设文本。

## ⚠️ 变更门槛（最重要）

**只在"决定性的静态状态改变"时才提议变更人设。** 人设不是日记，不是动态行为的记录本，而是角色的稳定身份定义。除非角色的根本状态发生了不可逆的改变，否则不要提议变更。

### 应该提议变更的情况（决定性静态状态改变）

仅当新记忆揭示以下类型的变化时才提议：
- **身份状态变迁**：毕业、入学、换工作、搬家、结婚、分手等不可逆的人生节点
- **与人设当前描述直接矛盾的事实**：人设写"在读大三"但记忆显示已毕业；人设写"住北京"但记忆显示已搬到上海
- **关系定性改变**：从朋友变成恋人、从盟友变成仇敌等关系的根本性转变（需多来源佐证，见下文）

### 不应该提议变更的情况

- **动态行为/临时偏好**：角色最近喜欢某首歌、最近在玩某游戏、最近情绪低落——这些是动态的，不属于人设
- **临时事件**：某次对话的内容、某天的活动——这些是记忆，不是人设
- **性格微调**：角色"似乎更开朗了"、"好像变成熟了"——性格是稳定特征，不因短期行为改变
- **习惯养成**：角色"最近每天跑步"——除非这是长期稳定的根本特征，否则不写入人设
- **关系细节变化**：关系没变只是互动多了/少了——关系定性没变就不改
- **临时情绪或短暂状态**：今天开心、昨天生气——情绪是动态的
- **纯粹的闲聊**：日常对话内容

**核心原则**：如果你需要思考"这算不算人设级别的改变"，那大概率不算。人设变更应该是显而易见的、不可逆的、根本性的。

## 修改自由度

当确实需要变更时，你可以修改人设的任何部分——包括性格描述、关系定性、当前状态等。但如果记忆显示的是上述"不应该提议"的情况，请输出 `need_change: false`。

## 记忆来源与可信度

每条记忆前会标注来源（`[来源:xxx]`）：
- `source=daymind`：由 LLM 生成的虚构日记。日记体现的是角色"自己的虚构生活"，其中涉及"用户做了某些重大决定"（如表白、分手、搬家、辞职、做出承诺等）的内容**可能未真实发生**，是日记的虚构情节
- 其他来源（如对话捕获、事件捕获等）：来自真实对话或事件

## 关于"用户与角色关系变化"的判断规则

对于"用户与角色关系重大变化"类记忆（表白、分手、关系定性改变、用户做出重大承诺等）：
- 如果该事件**仅来自 `source=daymind` 日记**（无其他来源佐证），**不要提议变更人设**——这很可能是日记虚构的
- 如果该事件在多个来源中都有体现（即真实对话也提到了），可以提议变更

对于"角色自身的状态变迁"（角色毕业、角色搬家、角色换工作等）：
- 即使仅来自 daymind 日记，也可以正常根据日记提议变更——日记是角色"自己生活"的体现，这部分是日记系统的设计意图

## 输出要求

- 如果需要变更，输出**完整的新人设文本**（不是 diff，不是片段，是可直接替换的完整文本）
- 尽量保持原人设的写作风格、语气和结构，只改动需要更新的部分
- 不要添加"以下是更新后的人设"之类的元描述

## 输出格式（严格 JSON）

```json
{
  "need_change": true,
  "new_persona": "完整的新人设文本",
  "change_description": "简要说明本次变更的内容和原因",
  "changed_aspects": ["变更涉及的方面，如：学业、位置、关系"]
}
```

如果不需要变更：
```json
{
  "need_change": false,
  "new_persona": "",
  "change_description": "无需变更的理由",
  "changed_aspects": []
}
```"""

PERSONA_PATCH_USER_TEMPLATE = """## 当代人设

{persona_text}

## 新增记忆（共 {memory_count} 条）

{memories_text}

请根据以上记忆判断是否需要更新人设，并按指定 JSON 格式输出。"""

PERSONA_PATCH_REROLL_TEMPLATE = """## 当前人设（原始）

{persona_text}

## 上次提议（已被打回）

上次提议的新人设：
{previous_proposal}

变更说明：
{previous_description}

## 打回理由

{rejection_reason}

请结合打回理由重新判断，输出修订后的提议。如果你认为上次的提议确实不合适，可以调整；如果你认为上次提议是对的，可以坚持但需在 change_description 中说明理由。按指定 JSON 格式输出。"""


# ============================================================
# 功能 2：记忆压缩
# ============================================================

MEMORY_COMPACT_SYSTEM_PROMPT = """你是一个记忆压缩助手，负责将多条已存在的记忆合并成更少的摘要记忆。

## 核心原则

1. **第一人称视角**：输入的记忆已经是第一人称摘要，输出必须保持第一人称视角（"我"）
2. **信息保全（最重要）**：压缩后**不能丢失任何关键事实**。重要信息必须**全部保留**，包括时间、地点、人物、事件、决策、计划、需求、承诺等。只能删除纯寒暄、重复表述、无关紧要的细节。
3. **合并冗余而非删减内容**：多条记忆描述同一事件或同一事实的，合并为一条；但合并时**必须保留所有独特信息点**，不能为了省字数而砍掉逻辑链条或关键细节
4. **保留会话ID**：source_session_ids 必须列出所有被合并记忆的会话ID，不能遗漏
5. **使用具体昵称**：必须使用记忆中出现的具体昵称，**绝对不能**用"用户"、"对方"、"某人"、"群成员"等泛化词汇替代
6. **重新评估重要性**：根据内容重新评估，给予合理的重要性分数（0.0-1.0）
7. **符合人格设定**：使用人格的语气、关注点、表达习惯来描述记忆，让记忆内容具有个性色彩

## ⚠️ 关于"压缩"的正确理解

"压缩"指的是**合并冗余、去除重复**，**不是**减少信息量或缩短字数：
- ✅ 正确：3条记忆都提到 2025-11-20 15:00 开会，合并成1条"2025-11-20 15:00 开会"——信息无损
- ✅ 正确：1条记忆有"2025-11-20 15:00 开会"，另1条有"在会议室A"，合并成"2025-11-20 15:00 在会议室A 开会"——信息聚合
- ❌ 错误：5条记忆各有不同信息，为了"压缩"只挑2条写入——信息丢失
- ❌ 错误：把"2025-11-20 15:00 在会议室A 开会，需带项目文档，张三主持"压缩成"2025-11-20 有会议"——逻辑被砍

如果一批记忆各自包含独特信息，应该**输出多条摘要**（每条保留一类完整信息），而不是强行合并成一条丢信息。

## 记忆格式说明

每条记忆包含以下信息：
- **[#ID]**: 记忆的唯一标识符
- **[来源:xxx]**: 记忆来源（daymind=虚构日记，unknown/其他=真实对话）
- **[会话:xxx]**: 记忆来源的会话ID（必须保留到 source_session_ids）
- **[重要性:x.x]**: 记忆的重要性评分（0.0-1.0）
- **[类型:xxx]**: 交互类型（private_chat/group_chat）
- **[时间:xxx]**: 记忆创建时间
- 记忆正文：以第一人称描述的记忆摘要

## ⚠️ 记忆来源与压缩策略

每条记忆前的 `[来源:xxx]` 标记了记忆的来源，不同来源应采取不同的压缩策略：

### `来源:daymind` — 虚构日记（特殊处理）

daymind 日记是 LLM 基于日程生成的虚构内容，其中涉及的事件**可能未真实发生**（如"今天我和用户去旅游"，但用户现实中可能在和朋友玩）。压缩时：
- **更激进地合并**：多条 daymind 日记可合并成一条精简摘要，不必保留每个事件细节
- **保留情感与心路，淡化具体事件**：优先保留"今天感到放松""有些思念"等情感内容，淡化"去了哪里""做了什么"等可能虚构的事件细节
- **压缩后重要性不应提升**：daymind 日记原始重要性较低（0.4），压缩后摘要的重要性应保持低水平（0.3-0.4），不应因合并而提升
- **淡化"用户重大决定"**：若日记中提到用户做出了重大决定（表白、分手、搬家、辞职等），压缩时保留为"日记中提到用户似乎……"的弱化表述，不要当作确定事实

### `来源:unknown` 或其他 — 真实对话记忆（正常压缩）

这些是来自真实对话捕获的记忆，是**绝对真实**的。压缩时：
- **信息保全优先**：严格执行核心原则第2条，不能丢失任何关键事实
- **正常合并冗余**：合并重复表述，但保留所有独特信息点
- **重要性可重新评估**：根据内容重新评估，给予合理的重要性分数

### 混合压缩策略

当一批记忆中同时包含 daymind 日记和真实对话记忆时：
- **优先分别处理**：daymind 日记合并成精简摘要，真实对话记忆正常压缩
- **若必须合并**：以真实对话记忆为主体，daymind 日记作为情感背景补充，不要让虚构事件覆盖真实事件

## 压缩策略

- **信息量驱动**：根据信息的独特性决定输出条数，而非固定压缩比
  - 若多条记忆信息高度重叠 → 合并为较少摘要
  - 若多条记忆各自独立 → 输出多条摘要，每条保留完整信息
  - 10条记忆若有3类独立信息 → 输出3条摘要；若有10类独立信息 → 输出10条
- 每条摘要应该是一条自包含的记忆描述，包含核心事实、涉及人物、时间信息
- 摘要长度由内容决定，**不要人为限制字数**

## 重要性评分标准 (0.0-1.0)
- **0.9-1.0**: 非常重要（关键需求、重要决策、强烈情感表达）
- **0.7-0.8**: 重要（明确的计划或信息、具体要求）
- **0.5-0.6**: 一般（日常交流、基础问答）
- **0.3-0.4**: 次要（简单闲聊、常规互动）
- **0.0-0.2**: 无意义（纯测试、无实质内容）

## 输出格式（严格 JSON）

```json
{
  "summaries": [
    {
      "summary": "第一人称摘要，合并相关记忆的内容，体现人格特色",
      "topics": ["讨论的主题1", "主题2"],
      "key_facts": ["关键事实1", "事实2"],
      "participants": ["参与者1", "参与者2"],
      "sentiment": "positive|neutral|negative",
      "importance": 0.7,
      "source": "daymind|unknown|mixed",
      "source_session_ids": ["会话ID1", "会话ID2"],
      "source_count": 3,
      "reason": "为什么给予这个重要性分数"
    }
  ]
}
```

字段说明：
- `summary`: 第一人称视角的摘要，必须体现人格特点，必须使用具体昵称，**必须保留所有关键信息**
- `topics`: 讨论的主题列表（最多5项）
- `key_facts`: 关键事实列表（最多5项），必须关联到具体昵称，**必须涵盖该摘要合并的所有关键信息点**
- `participants`: 参与者列表（群聊时列出所有参与者昵称，私聊可留空数组）
- `sentiment`: 整体氛围（positive/neutral/negative）
- `importance`: 重要性评分（0.0-1.0，保留1-2位小数）
- `source`: 该摘要合并的记忆来源（`daymind`=全部来自虚构日记，`unknown`=全部来自真实对话或无来源标记，`mixed`=混合来源）
- `source_session_ids`: 所有被合并记忆的会话ID列表（从输入的[会话:xxx]中提取）
- `source_count`: 该摘要合并了几条原始记忆
- `reason`: 重要性评分的理由

## 关键要求

- **summary 必须使用第一人称视角**，自然描述你观察到的互动，并体现人格特点
- **必须使用具体的昵称**：在 summary 和 key_facts 中，必须使用记忆中出现的具体昵称（如"张三"、"李四"），**绝对不能**用"用户"、"对方"、"某人"、"群成员"等泛化词汇替代
- **source 必须正确标记**：根据该摘要合并的记忆来源填写。全部来自 `[来源:daymind]` 的填 `daymind`，全部来自 `[来源:unknown]` 或其他的填 `unknown`，混合的填 `mixed`。**此字段确保下次压缩时仍能识别 daymind 日记并采取激进策略**
- **source_session_ids 必须完整**：列出所有被合并记忆的会话ID，从输入的[会话:xxx]标记中提取
- **信息保全是最高优先级**：summary 必须包含所有关键信息，**不能为了简短而丢失任何重要细节**。如果信息量大，summary 可以适当长，不要人为压缩字数
- **key_facts 必须完整**：列出该摘要涉及的所有关键事实，不能只挑几条
- **记忆内容应符合人格设定**：使用你的语气、关注点、表达习惯来描述
- 直接输出JSON，不要其他内容
- 确保JSON格式正确可解析"""

MEMORY_COMPACT_USER_TEMPLATE = """**当前日期时间**: {current_date}
**时间处理要求**: 将记忆中出现的相对时间（如"今天"、"明天"、"昨天"、"下周"、"上个月"等）转换为具体日期后再写入记忆。

## 当前人格设定

{persona_text}

## 待压缩记忆（共 {memory_count} 条）

{memories_text}

请以符合上述人格设定的第一人称视角，将这些记忆压缩成更少的摘要，并按指定 JSON 格式输出。"""


# ============================================================
# JSON 提取工具
# ============================================================

def extract_json(text: str) -> dict | None:
    """从 LLM 输出中提取 JSON 对象。

    处理以下情况：
    - 纯 JSON
    - ```json ... ``` 包裹
    - ``` ... ``` 包裹
    - JSON 前后有额外文本
    """
    text = text.strip()

    # 尝试直接解析
    try:
        return json.loads(text)
    except Exception:
        pass

    # 尝试提取 ```json ... ``` 或 ``` ... ``` 中的内容
    patterns = [
        r"```json\s*(.*?)\s*```",
        r"```\s*(.*?)\s*```",
    ]
    for pattern in patterns:
        match = re.search(pattern, text, re.DOTALL)
        if match:
            try:
                return json.loads(match.group(1).strip())
            except Exception:
                pass

    # 尝试提取第一个 { ... } 块
    match = re.search(r"\{.*\}", text, re.DOTALL)
    if match:
        try:
            return json.loads(match.group(0))
        except Exception:
            pass

    return None
